# Topic 01: Language

How to translate basic ideas into code.

*   **[00-HelloWorld](00-HelloWorld)**: Understanding the build process, the `main` function, and how C++ code becomes an executable.
*   **[01-Integers](01-Integers)**: Working with whole numbers, bitwise operations, and understanding binary representation and overflow.
*   **[02-Control-Flow](02-Control-Flow)**: Controlling program execution with `if`/`else`, loops (`while`, `for`), and `switch` statements.
*   **[03-Floats](03-Floats)**: Working with real numbers, understanding floating-point precision, and special values like Infinity and NaN.